spring:
    datasource:
      hikari:
        jdbc-url: "jdbc:postgresql://localhost:5432/postgres"
        maximum-pool-size: 10
        driver-class-name: org.postgresql.Driver
      username: postgres
      password: postgres
      url: "jdbc:postgresql://localhost:5432/postgres"
      driver-class-name: org.postgresql.Driver

    flyway:
      enabled: true
      ignore-missing-migrations: true
      locations: classpath:/migration
      baseline-on-migrate: true
      baseline-description: Flyway Scripts
      url: ${spring.datasource.url}
      user: ${spring.datasource.username}
      password: ${spring.datasource.password}

    mail:
      host: smtp.gmail.com
      port: 465
      username: zhdv271@gmail.com
      password: guuqvpuclwvbnlsu
      properties:
        mail:
          smtp:
            starttls:
              enable: true
            auth: true

#    mail:
#      host: smtp.gmail.com
#      port: 465
#      username: zhdv271@gmail.com
#      password: guuqvpuclwvbnlsu
#      protocol: smtp
#      tls: true
#      properties.mail.smtp:
#        auth: true
#        starttls.enable: true
#        ssl.trust: smtp.gmail.com


jwtconfig:
  secret: secret
  expiration: 6000000


server:
  port: 8081

amazon:
  serverUrl: "https:/jd2aws.s3.eu-central-1.amazonaws.com"
  awsAccessKeyId:
  awsSecretKey:
  folder: car
  bucket: jd2aws
  region: eu-central-1
